kind: Ingress
apiVersion: extensions/v1beta1
metadata:
  name: app-ingress
spec:
 rules:
 - http:
     paths:
     - path: /*
       backend:
         serviceName: kubeapp-production-service
         servicePort: 80